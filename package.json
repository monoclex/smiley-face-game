{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "lerna run lint --no-bail --parallel",
    "lint:fix": "lerna run lint:fix --no-bail --parallel",
    "serve": "lerna run serve --parallel"
  },
  "dependencies": {}
}
