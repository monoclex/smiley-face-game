{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/**",
    "scripts"
  ],
  "scripts": {
    "test": "pnpm run --recursive --parallel test",
    "tsc": "pnpm run --recursive --parallel tsc",
    "build:dev": "pnpm run --recursive --parallel build:dev",
    "build:prod": "pnpm run --recursive --parallel build:prod",
    "lint": "pnpm run --recursive --parallel lint",
    "lint:fix": "pnpm run --recursive --parallel lint:fix",
    "serve": "pnpm run --recursive --parallel serve "
  }
}
